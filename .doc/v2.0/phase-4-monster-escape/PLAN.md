# P4 — 怪獸逃跑重設計 · PLAN

| # | 里程碑 | 產出 | Task |
|---|--------|------|------|
| M1 | 邏輯層 | `processForgottenEscapes` + `markReviewCompleted` + LevelProgress 擴充；刪除 `processSpontaneousEscapes`；測試 | TASK-03a |
| M2 | UI 接線 | VocabAdventureMap 逃跑劇情 + SmartDailyReview 完成抓回慶祝 + 文案 | TASK-03b |

順序：M1（本 chunk，全自動測）→ **文案 tone GATE（使用者）** → M2（UI，需使用者手動驗收）。
退出：M1 達 Task DoD。M2 前必須先過文案/是否顯示逃跑的使用者確認。
